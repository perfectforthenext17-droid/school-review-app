1放宽对于图片的限制
上传的图片大于等于四》可通过（忽略）

2后台ui的美化，以及后台流畅性的优化（待定/次要）

3增加文件只能提交2次的提示（否则进入人工复审）////(已完成)
若用户通过密码进入后端管理员模式》程序在管理员端时：
文件提交页面窗口新增一个提交次数重置按钮
（该按钮仅仅重置当前被选择班级上传文件的次数，
不影响原来的未提交/提交成功/被驳回的状态以及其他状态），
注意，只有登入管理员后台端才会有这个效果

and请勿随意上传文件 该程序以接入ai审核，并且会进行人工抽查，若发现消极提交态度，则文件作废，

4***人工审核，方案：文件上传次数超过限制/上传失败》直接驳回不予通过

5***人工抽查，方案：对后续打印出的压缩包进行随机检查（增加一项环节，多一份人力）

6一键通过///（已完成）
若用户通过密码进入后端管理员模式》
程序在管理员端时：
文件提交页面窗口新增一个通过按钮
（该按钮可以直接通过被选择班级的文件提交情况，并将按钮直接变为绿色，并且同步到后台），
注意，只有登入管理员后台端才会有这个效果，
这个功能的作用是为了防止程序的偶然失误造成的文件审查失败，
如果这样改动，
不会影响后面文件打印的环节
（若该班级提交文件为空但显示通过状态，可直接无视，接着打包之后的文件）

7保留未通过原因（前后端）
加入高一高二高三的弹窗

8部分规则的修改///（已完成）
***已完成a/上传文件的次数变为两次
***已完成b/当用户第一次提交文件时，若审核通过，则正常打入数据库，若审核不通过，显示失败原因并驳回一次，不上传数据库，给予一次更正机会，当用户第二次提交文件时，若审核通过
，则正常打入数据库，若不通过，则显示驳回原因，但文件将会被锁定并上传数据库，并参加之后的压缩包打印环节
***已完成c/在后端添加一个功能：
当我点击某个被驳回班级时，
可以弹出该班级被驳回原因，若该班级是第一次被驳回，
那么只显示原因，若是第二次被驳回，
则连同两次原因一起显示，
并增加一个按钮，可以直接从数据库中调出第二次被锁定的文件，供管理员查看
***已完成d/若登入管理员模式，
在前端新添加一个按键：当管理员点击对应班级时，可以查看到对应文件（注意，只能查看已通过和第二次未通过被打入数据库的文件）

9公告功能的插入
这份公告可以在后台编辑，
当用户初次进入网站时，若管理员发布了公告，则会弹出一份相应的显示框（ui要符合源程序的风格），
当用户自行阅读完以后，可点击下方的”我知道了“按键，
随后进入网站



